// Jest setup. Imported for side-effects (e.g. jest-dom matchers).
// @testing-library/jest-dom is only needed for component tests; for pure
// unit tests on Zod schemas we don't need it, but loading it is harmless.
import "@testing-library/jest-dom";
